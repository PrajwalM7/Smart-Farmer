import React from "react"; import { useNavigate } from "react-router-dom"; import "../App.css"; function GovernmentSchemes() { const navigate = useNavigate(); const schemes = [ { name: "PM-KISAN", benefit: "₹6000 per year assistance", }, { name: "PMFBY", benefit: "Crop insurance support", }, { name: "Soil Health Card", benefit: "Free soil testing", }, { name: "Kisan Credit Card", benefit: "Low interest loans", }, { name: "PMKSY", benefit: "Irrigation support", }, ]; return ( <div className="schemes-page"> <div className="schemes-card"> <div className="schemes-header"> <h1 className="schemes-title"> 🌾 Government Schemes </h1> <button className="back-btn" onClick={() => navigate("/dashboard") } > ← Back </button> </div> {schemes.map((scheme, index) => ( <div className="scheme-item" key={index} > <h3>{scheme.name}</h3> <p>{scheme.benefit}</p> </div> ))} </div> </div> ); } export default GovernmentSchemes;